# ----------------------------------------
# Application Info
# ----------------------------------------
spring.application.name=project-management-system
server.port=8080

# ----------------------------------------
# MongoDB Configuration
# ----------------------------------------
spring.data.mongodb.uri=mongodb://localhost:27017/pmsdb

# ----------------------------------------
# Disable H2 & JPA Configuration
# ----------------------------------------
# (comment out or remove to avoid conflicts)
# spring.datasource.url=jdbc:h2:file:./data/pmsdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# ----------------------------------------
# Logging (Optional: helps debug Spring Security issues)
# ----------------------------------------
logging.level.org.springframework.security=DEBUG
